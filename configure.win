#!/bin/sh
set -eu

# Rtools provides the compiler plus target-aware pkg-config wrappers for its
# static libxml2 and zlib libraries. Keep one build implementation so Windows,
# Unix, and webR validate and package identical DuckDB extension sources.
ROOT=`CDPATH= cd -- "$(dirname -- "$0")" && pwd`
exec "$ROOT/configure" "$@"
