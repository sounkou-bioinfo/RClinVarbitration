% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{rclinvarbitration_download_clinvar}
\alias{rclinvarbitration_download_clinvar}
\title{Download official ClinVar source files}
\usage{
rclinvarbitration_download_clinvar(
  release = "latest",
  file = "vcv_xml",
  cache_dir = tools::R_user_dir("RClinVarbitration", "cache"),
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{release}{\code{"latest"} or a monthly archive in \code{"YYYY-MM"} form.}

\item{file}{One or more of \code{"vcv_xml"}, \code{"submission_summary"}, and
\code{"variant_summary"}.}

\item{cache_dir}{Directory in which downloaded files are cached.}

\item{overwrite}{Download even when a valid cached file is present?}

\item{quiet}{Passed to \code{\link[utils:download.file]{utils::download.file()}}.}
}
\value{
A named character vector of local paths. The \code{download} attribute is
a data frame containing source URLs, MD5 values when available, and cache
hit status.
}
\description{
Downloads either the current ClinVar release or a named monthly archive from
the NCBI HTTPS service. The VCV XML file is the normal input to
\code{\link[=rclinvarbitration_import_xml]{rclinvarbitration_import_xml()}}. The two flat files are optional,
validation-only inputs to
\code{\link[=rclinvarbitration_reproduce_clinvarbitration_parquet]{rclinvarbitration_reproduce_clinvarbitration_parquet()}}.
}
\details{
Existing files form a local cache. Current files and archived VCV XML files
are checked against NCBI's MD5 sidecar; a matching file is reused and a stale
or corrupt file is replaced only after a temporary download is verified.
NCBI does not publish MD5 sidecars
beside archived flat files, so those files are reused by name unless
\code{overwrite = TRUE}.

A complete VCV release is several gigabytes. The default cache location is
returned by \code{tools::R_user_dir("RClinVarbitration", "cache")}. The function
raises R's download timeout to at least the value of option
\code{RClinVarbitration.download_timeout} (7200 seconds by default) for the call.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
xml <- rclinvarbitration_download_clinvar(release = "latest")
xml_2026_03 <- rclinvarbitration_download_clinvar(release = "2026-03")
\dontshow{\}) # examplesIf}
}
