# Initialize the ClinVar relational schema

Initialize the ClinVar relational schema

## Usage

``` r
rclinvarbitration_init(con)
```

## Arguments

- con:

  A DuckDB DBI connection.

## Value

`con`, invisibly.
